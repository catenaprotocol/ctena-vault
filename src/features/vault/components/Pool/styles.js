const styles = theme => (

  {

  container: {
    marginBottom: '24px',
    border: '0.5px solid ' + theme.palette.background.border,
    borderRadius:'10px',
  },
  accordion: {
    width: '100%',
    backgroundColor: theme.palette.background.primary,
    borderRadius:'10px',
  },
  divider: {
    margin: '0 30px',
  },
});

export default styles;
